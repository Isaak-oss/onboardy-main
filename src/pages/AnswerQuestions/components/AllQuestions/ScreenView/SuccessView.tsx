import React from "react";
import { QuestionsViewTypes } from "../AllQuestions.tsx";

const SuccessView = ({ setView }: { setView: (view: QuestionsViewTypes) => void }) => {
  return <div>Answer success</div>;
};

export default SuccessView;
